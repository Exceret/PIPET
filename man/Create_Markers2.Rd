% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_Create_Markers2.R
\name{Create_Markers2}
\alias{Create_Markers2}
\title{Create Markers on Log2-transformed Bulk Data}
\usage{
Create_Markers2(
  bulk_data,
  colData,
  class_col = NULL,
  lg2FC = 1L,
  p.adjust = 0.05,
  show_lg2FC = TRUE,
  ...
)
}
\arguments{
\item{bulk_data}{A numeric matrix of log2-transformed expression values (e.g., log2(x + 1)), with rows = genes and columns = samples.
Must contain \strong{non-integer (decimal) values}, if all integers, function aborts and suggests using \code{Create_Markers()}.}

\item{colData}{A \code{data.frame} with rows corresponding to columns of \code{bulk_data}.}

\item{class_col}{The name of the column in \code{colData} indicating subclass labels.}

\item{lg2FC}{The absolute log2 fold change cutoff (default: 1). Genes with |log2FC| >= lg2FC are retained.}

\item{p.adjust}{Adjusted p-value cutoff (default: 0.05).}

\item{show_lg2FC}{Whether to include log2 fold change in output (default: TRUE).}

\item{...}{Additional arguments: \code{verbose} (default: \code{TRUE}), \code{seed} (ignored but accepted), \code{parallel} (passed to \code{limma::eBayes} if \code{trend = TRUE} uses \code{parallel = TRUE} via \code{BiocParallel}).}
}
\value{
A \code{data.frame} with columns:
\item{genes}{Differential genes passing cutoffs.}
\item{class}{The subclass to which the marker gene is upregulated.}
\item{log2FoldChange}{(if \code{show_lg2FC = TRUE}) Signed log2 fold change.}
}
\description{
Based on differential expression analysis using limma (suitable for log2-transformed normalized expression data, e.g., log2(TPM+1), log2(FPKM+1), or log2(counts+1)),
the feature vector of each subclass is established. The input bulk_data must be \strong{log2-transformed continuous values}, not raw counts.
At least two subclasses are required. Subclasses will be converted to factors if not already.
}
